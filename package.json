{
  "name": "projectmanager",
  "version": "1.0.0",
  "private": true,
  "description": "Cadence — Boards / Base / Time suite with MongoDB-backed state.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "mongodb": "^7.4.0",
    "sob-connect-sdk": "^0.1.0"
  }
}
